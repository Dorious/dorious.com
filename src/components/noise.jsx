import React from 'react';
import PropTypes from 'prop-types';

import './noise.less';

const Noise = () => {
  return (
    <div className="noise"></div>
  );
};

Noise.propTypes = {};

export default Noise;